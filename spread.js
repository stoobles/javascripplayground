let position = { x:0, y:0};
let dimensions = {width: 100, height: 75};
let rect = {...position, ...dimensions};
console.log(`rect.x: ${rect.x}, rect.y: ${rect.y}, rect.width: ${rect.width}, rect.length: ${rect.height}`);